\documentclass[conference]{IEEEtran}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}
\usepackage{siunitx}
\usepackage{url}
\usepackage{xcolor}
\usepackage{balance}

\title{Curriculum Reinforcement Learning for Variable-Height Balancing of a 10-Actuated Wheeled Biped Robot}

\author{
\IEEEauthorblockN{Van Thuong Nguyen}
\IEEEauthorblockA{
Independent Researcher\\
Email: vanthuong180702t@gmail.com
}
}

\begin{document}

\maketitle

\begin{abstract}
This paper presents a curriculum reinforcement learning framework for training a 10-actuated wheeled biped robot to balance at variable commanded torso heights.
Each leg has five actuated joints: hip roll, hip yaw, hip pitch, knee, and wheel.
The control policy is trained in MuJoCo MJX with JAX-accelerated vectorized simulation
using proximal policy optimization (PPO).
The policy receives a 41-dimensional observation that includes proprioceptive state,
a normalized height command, and a yaw-error signal, and outputs joint-level targets
that are converted to actuator torques through a PID low-level controller.
To stabilize learning across a wide range of body heights, we employ a within-stage
height curriculum that progressively expands the commanded height range from
near the nominal standing posture toward deep crouching configurations, gated
by a reward-threshold criterion evaluated on a dedicated greedy evaluation pass
every $I_{\mathrm{eval}} = 2$ training updates.
A key component of the reward design is a height-consistent natural pose term
that maps commanded height to target hip-pitch and knee angles via
piecewise-linear interpolation, preventing degenerate postures that satisfy
height objectives through unnatural joint configurations.
We additionally implement a classical LQR baseline built on a linearized
two-wheeled inverted pendulum (TWIP) model with height-dependent IK,
providing a fair reference for the near-nominal operating range.
The framework also includes a multi-stage macro-curriculum structure
(stages: \texttt{balance}, \texttt{stand\_up}, \texttt{balance\_robust})
for future extension, though the present work focuses on the variable-height
balancing stage.
We describe the full system design, reward formulation, curriculum strategy,
LQR baseline, and evaluation protocol, and present the framework as a foundation
for further training and experimental validation.
% [TODO-RESULT: replace this sentence with quantitative summary once training is complete]
\end{abstract}

\begin{IEEEkeywords}
Wheeled biped robot, reinforcement learning, curriculum learning, variable-height balancing,
PID control, LQR baseline, MuJoCo MJX.
\end{IEEEkeywords}

% ============================================================
\section{Introduction}
% ============================================================
Wheeled biped robots combine the efficiency of wheel-based locomotion with
the adaptability of legged morphology.
Compared with conventional legged systems, wheeled bipeds can achieve
lower-cost motion on flat terrain while exploiting articulated legs to modify
body posture, regulate center-of-mass height, and improve disturbance rejection.
However, these benefits come at the cost of more challenging control:
balancing at different commanded heights requires managing the nonlinear
coupling between wheel dynamics, torso attitude, and multi-joint leg motions.

Classical control approaches~\cite{klemm2020lqrwbc} can stabilize wheeled bipeds
near a nominal operating point, but become increasingly inaccurate as the
robot moves away from the linearization height.
Recent progress in reinforcement learning (RL) has shown that robust control
policies can be learned directly in simulation for complex robotic
systems~\cite{hwangbo2019learning,lee2020learning,rudin2022walk}.
Nevertheless, training a single policy to balance across a wide range of
body heights is difficult due to poor sample efficiency and the risk of
reward exploitation.
In particular, simple height-tracking rewards may allow policies to satisfy
torso height objectives through unnatural or unstable joint configurations
rather than through physically meaningful postures.

To address these challenges, we propose a curriculum reinforcement learning
framework for a 10-actuated wheeled biped robot.
The method explicitly conditions the policy on a target torso height and
uses a within-stage height curriculum to progressively expand the difficulty
from near-nominal standing to deep crouching.
A height-consistent posture reward regularizes the joint configuration as a
function of the commanded height, improving training stability and posture quality.
We also implement a classical LQR baseline that operates on the same observation
interface and PID low-level controller as the RL policy, providing an
interpretable reference for the regime where linear assumptions hold.

The main contributions of this work are:
\begin{itemize}
    \item A height-conditioned RL framework for variable-height balancing of
    a 10-actuated wheeled biped, with PID low-level control bridging policy
    outputs to actuator torques.
    \item A within-stage height curriculum that expands the commanded height
    range based on a greedy evaluation pass gated at a reward-threshold criterion,
    stabilizing learning across the full feasible height range.
    \item A height-consistent natural pose reward that ties hip-pitch and knee
    joint targets to the commanded torso height via piecewise-linear
    interpolation, preventing degenerate balancing strategies.
    \item A classical LQR baseline built on a linearized TWIP model with
    height-dependent inverse kinematics, serving as an interpretable
    comparison for the near-nominal height regime.
    \item A quantitative evaluation benchmark with ten scenario groups and
    ten per-scenario metrics, enabling rigorous comparison between the RL
    policy and the LQR baseline.
    % [TODO-CLAIM: the experimental claims above are structural; quantitative
    % superiority of RL over LQR must only be stated after experiments are complete]
\end{itemize}

The framework additionally includes a multi-stage macro-curriculum
(\texttt{balance} $\to$ \texttt{stand\_up} $\to$ \texttt{balance\_robust})
and a quantitative offline evaluation benchmark suite with ten scenario
groups and ten per-scenario metrics.
These extensions are described in this paper, though their full experimental
results remain as future work.

The rest of this paper is organized as follows.
Section~II reviews related work.
Section~III presents the robot platform and simulation setup.
Section~IV formulates the learning problem.
Section~V describes the reward design.
Section~VI presents the curriculum strategy.
Section~VII describes the classical LQR baseline.
Section~VIII details the training pipeline and evaluation protocol.
Section~IX is reserved for experimental results.
Section~X discusses limitations and future work.
Section~XI concludes the paper.

% ============================================================
\section{Related Work}
\label{sec:related}
% ============================================================

\subsection{Classical and Model-Based Control for Wheeled Bipeds}
Wheeled bipedal robots combine the energy efficiency of wheel-based locomotion
with the postural adaptability of articulated legs.
Early and influential work by Klemm~\emph{et al.}~\cite{klemm2019ascento}
introduced Ascento, a two-wheeled jumping robot demonstrating active balance
through LQR-based stabilization around an inverted pendulum model.
Subsequent work~\cite{klemm2020lqrwbc} extended this to an LQR-assisted
whole-body controller with kinematic loops.
Hsu~\emph{et al.}~\cite{hsu2022wbrfuzzy} applied fuzzy logic control to
wheeled biped balance regulation.
Li~\emph{et al.}~\cite{li2021wheelbiped} investigated dynamic motion control
for wheel-biped transformable robots.
The DIABLO platform~\cite{diablo2024} demonstrated six-DOF direct-drive
wheeled biped control using cascaded LQR.
While these model-based approaches provide stability guarantees near an
operating point, they require accurate dynamics models and degrade when the
robot deviates significantly from the linearization configuration---a
limitation that becomes severe under variable-height commands.

\subsection{Deep Reinforcement Learning for Legged Locomotion}
Deep RL has produced robust locomotion policies for complex legged systems.
Hwangbo~\emph{et al.}~\cite{hwangbo2019learning} introduced a learned
actuator network that accurately models motor dynamics, enabling sim-to-real
transfer of agile skills on ANYmal.
Lee~\emph{et al.}~\cite{lee2020learning} demonstrated blind proprioceptive
locomotion over challenging outdoor terrain using a teacher-student
distillation pipeline.
Kumar~\emph{et al.}~\cite{kumar2021rma} proposed Rapid Motor Adaptation (RMA),
a two-stage approach that trains an adaptation module online to handle
unseen dynamics such as varying payload and slippery floors.
Rudin~\emph{et al.}~\cite{rudin2022walk} showed that massively parallel
GPU-based simulation (4,096 environments in Isaac Gym) can train walking
policies in minutes using a terrain curriculum closely related to ours.
Miki~\emph{et al.}~\cite{miki2022wild} further demonstrated perception-aware
locomotion over kilometer-scale outdoor terrain using attention-based
proprioceptive encoders.
These works establish that RL can scale to high-dimensional contact-rich
control problems; our work adapts this paradigm to the specific challenge of
variable-height wheeled-biped balancing.

\subsection{Reinforcement Learning for Wheeled-Legged Robots}
More recently, RL has been applied to wheeled-legged hybrid platforms.
Lee~\emph{et al.}~\cite{lee2024robustnavigation} presented a full autonomy
stack for ANYmal-on-wheels, achieving 20\,km/h driving with 83\% reduced
cost-of-transport.
Vollenweider~\emph{et al.}~\cite{vollenweider2023advancedskills} trained diverse
locomotion skills for wheeled-legged robots through adversarial motion priors.
Chamorro~\emph{et al.}~\cite{chamorro2024stairclimbing} used asymmetric
actor-critic RL with privileged terrain information for blind stair climbing
on Ascento, successfully transferring to hardware for 15\,cm step negotiation.
Zhu~\emph{et al.}~\cite{whleaper2024} introduced Whleaper, a 10-DOF wheeled
biped most similar to our platform, demonstrating both LQR and PPO control
for multimodal locomotion.
Zhu and Hayashibe~\cite{zhu2022hierarchical} applied hierarchical RL for
path tracking on a wheeled biped with a sim-to-real transfer pipeline.
In contrast to these platforms---which either fix body height or focus on
forward locomotion---our work specifically addresses \emph{variable-height
stationary balance}, requiring the policy to operate stably across a 30\,cm
height range while maintaining postural integrity.

\subsection{Curriculum Learning for Robot Control}
Curriculum learning, introduced by Bengio~\emph{et al.}~\cite{bengio2009curriculum},
organizes training from easy to hard examples to improve convergence
and generalization.
In robotics, Rudin~\emph{et al.}~\cite{rudin2022walk} demonstrated a
terrain difficulty curriculum that substantially improves traversal capability.
The Automatic Curriculum Learning (ACL) survey by
Portelas~\emph{et al.}~\cite{portelas2020acl} systematizes curriculum strategies
by learning progress, distinguishing goal-parameterized curricula from
task-space curricula.
Teacher algorithms such as ALP-GMM~\cite{portelas2020teacher} generate
parameterized task distributions that maximize learning progress in
continuous task spaces.
POET~\cite{wang2019poet} co-evolves environments and agents to produce
increasingly complex curricula.
Our within-stage height curriculum is a monotone threshold-gated curriculum:
the commanded height range expands only when a greedy evaluation pass
confirms the policy has mastered the current range, directly addressing
the instability that arises when a single policy must balance across
configurations with qualitatively different inverted-pendulum dynamics.

\subsection{Domain Randomization and Sim-to-Real Transfer}
Sim-to-real transfer for locomotion policies relies on domain randomization
to cover the reality gap.
Tobin~\emph{et al.}~\cite{tobin2017domain} established domain randomization
as a viable transfer strategy.
Peng~\emph{et al.}~\cite{peng2018simtoreal} demonstrated dynamics
randomization for robust quadruped locomotion on physical hardware.
Tan~\emph{et al.}~\cite{tan2018simtoreal} showed that randomizing actuator
parameters and adding observation noise enables Minitaur to transfer
sim-trained trotting gaits to the real robot without fine-tuning.
Our framework adopts these established protocols---randomizing friction,
mass properties, motor strength, and adding IMU/encoder noise and
action delay---to prepare the trained policy for future hardware deployment.

% ============================================================
\section{Robot Platform and Simulation Setup}
% ============================================================

\subsection{Robot Morphology}
The robot considered in this work is a 10-actuated wheeled biped with five
actuated joints on each leg: hip roll, hip yaw, hip pitch, knee, and wheel.
The platform has a total mass of approximately \SI{8.1}{kg},
thigh length of \SI{0.26}{m},
shin length of \SI{0.28}{m},
wheel radius of \SI{0.06}{m},
and hip width of \SI{0.23}{m}.
The sensing system includes one IMU and ten joint encoders.
A physical robot with the same kinematic structure exists and is intended
for future sim-to-real transfer, though hardware experiments are not part
of the present work.

Let the joint vector be defined as
\begin{equation}
q_j =
\begin{bmatrix}
q_{\mathrm{hr}}^L &
q_{\mathrm{hy}}^L &
q_{\mathrm{hp}}^L &
q_{\mathrm{kn}}^L &
q_{\mathrm{wh}}^L &
q_{\mathrm{hr}}^R &
q_{\mathrm{hy}}^R &
q_{\mathrm{hp}}^R &
q_{\mathrm{kn}}^R &
q_{\mathrm{wh}}^R
\end{bmatrix}^\top,
\end{equation}
where the superscripts $L$ and $R$ denote the left and right legs, respectively.

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/annotated_robot_joints.png}
    \caption{Mechanical structure and joint layout of the wheeled biped robot.
    Each leg has five actuated joints: hip roll (HR), hip yaw (HY), hip pitch (HP),
    knee (KN), and drive wheel (WH).
    The left leg is annotated; the right leg is mirror-symmetric.
    The hip and knee joints enable variable-height posture regulation;
    the wheel joints provide balancing torque.}
    \label{fig:robot_joint_layout}
\end{figure}

Fig.~\ref{fig:robot_joint_layout} illustrates the mechanical structure
and joint layout.
The articulated hip and knee joints enable variable-height posture regulation,
while the wheel joints provide balancing capability through active torque generation.

\subsection{Simulation Environment}
All policies are trained in MuJoCo MJX~\cite{mujoco,mujoco_mjx_docs}
with JAX-based~\cite{jax} vectorized simulation.
The physics simulation runs at \SI{500}{Hz} with timestep
\begin{equation}
\Delta t_{\mathrm{sim}} = \SI{0.002}{s},
\end{equation}
while the policy is queried at \SI{50}{Hz},
\begin{equation}
\Delta t_{\pi} = \SI{0.02}{s}.
\end{equation}
Each policy action is therefore held constant for ten simulation substeps.
Training is parallelized across 4096 environments on GPU.

Domain randomization~\cite{tobin2017domain} is applied during training
to improve robustness: body masses are perturbed by $\pm 15\%$,
ground friction coefficients by $\pm 40\%$, and joint damping
coefficients by $\pm 30\%$ relative to their nominal values.
Randomization is applied per episode at reset time.
In the base \texttt{balance} stage, no external push disturbances are applied
($F_{\mathrm{push}} = 0$);
random joint-position noise of $\pm 0.10\,\mathrm{rad}$ is added at reset
to expose the policy to varied initial postures.
Push disturbances (\SI{40}{N} applied over 8 control steps, $\approx\SI{0.16}{s}$)
are introduced only in the \texttt{balance\_robust} fine-tuning stage.

Table~\ref{tab:domain_rand} summarizes the randomized parameters and their
ranges, following established sim-to-real protocols~\cite{tobin2017domain,peng2018simtoreal}.
Each parameter is sampled independently per episode from a uniform distribution
over the stated range, with the nominal value at the midpoint unless noted.

\begin{table}[t]
\centering
\caption{Domain randomization parameters applied during training.
All parameters are sampled uniformly per episode.
Sensor noise is sampled independently per step.}
\label{tab:domain_rand}
\begin{tabular}{l l l}
\toprule
Parameter & Range & Unit \\
\midrule
\multicolumn{3}{l}{\textit{Dynamics}} \\
Friction coefficient   & $[0.5,\; 1.5]$         & --- \\
Total body mass        & $\pm 15\%$ of nominal   & kg \\
Motor strength         & $[0.8,\; 1.2]\times$    & --- \\
Joint damping          & $[0.8,\; 1.2]\times$    & --- \\
CoM offset (x, y)      & $[-0.02,\; 0.02]$       & m \\
\midrule
\multicolumn{3}{l}{\textit{Sensors \& Actuation}} \\
IMU angular velocity noise  & $\mathcal{N}(0,\,0.05^2)$ & rad/s \\
Joint encoder noise         & $\mathcal{N}(0,\,0.01^2)$ & rad \\
Linear velocity noise       & $\mathcal{N}(0,\,0.05^2)$ & m/s \\
Action delay                & 1 step ($\approx$20\,ms)   & --- \\
\midrule
\multicolumn{3}{l}{\textit{Initial Conditions}} \\
Initial body tilt           & $[-0.05,\; 0.05]$   & rad \\
Initial joint offset        & $[-0.05,\; 0.05]$   & rad \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Low-Level PID Control}
\label{sec:pid}
Rather than mapping policy outputs directly to actuator torques, we employ
a PID low-level controller that converts normalized policy targets into
torque commands.
This decouples the policy from raw torque generation and simplifies the
learning problem.

The policy outputs $a_t \in [-1,1]^{10}$, which are interpreted differently
depending on the joint type:
\begin{itemize}
    \item \textbf{Leg joints} (hip roll, hip yaw, hip pitch, knee):
    the policy output is interpreted as a position target.
    A PD+I controller then computes the torque:
    \begin{equation}
    \tau_j = K_p^j (q_j^* - q_j)
             + K_d^j (0 - \dot{q}_j)
             + K_i^j \int (q_j^* - q_j)\, dt,
    \end{equation}
    where $q_j^*$ is the position target, $q_j$ and $\dot{q}_j$ are the
    measured joint position and velocity, and $K_p^j$, $K_d^j$, $K_i^j$
    are per-joint gains.
    The integral term includes anti-windup clamping
    ($\pm 0.35\,\mathrm{rad}{\cdot}\mathrm{s}$).
    \item \textbf{Wheel joints}: the policy output is interpreted as a
    velocity target.
    A PI controller computes the torque:
    \begin{equation}
    \tau_w = K_p^w (\dot{q}_w^* - \dot{q}_w)
             + K_i^w \int (\dot{q}_w^* - \dot{q}_w)\, dt,
    \end{equation}
    where $\dot{q}_w^*$ is the velocity target.
    The derivative gain is masked to zero ($K_d^w = 0$) because wheel
    velocity-error derivative is the joint acceleration rather than
    joint velocity, making standard $K_d$ semantics ill-defined for
    velocity-control joints.
\end{itemize}

An action-smoothing filter with coefficient $\alpha = 0.4$
blends the current and previous policy output before passing to the PID
controller, reducing command chattering.
Optionally, an action delay of $n_{\mathrm{delay}}$ control steps can
model CAN-bus latency during sim-to-real preparation
(default: $n_{\mathrm{delay}} = 0$ for simulation training).
All torque outputs are clipped to the actuator limits.
The per-joint gains are given in Table~\ref{tab:pid_gains}.

\begin{table}[t]
\centering
\caption{Per-joint PID gains. Joint order: hip roll (HR), hip yaw (HY),
hip pitch (HP), knee (KN), wheel (WH). Gains are identical for left
and right legs.
The wheel $K_d$ is masked to zero internally regardless of the table value.}
\label{tab:pid_gains}
\begin{tabular}{l r r r}
\toprule
Joint & $K_p$ & $K_i$ & $K_d$ \\
\midrule
HR   & 55.0 & 0.8 & 3.0 \\
HY   & 40.0 & 0.4 & 2.0 \\
HP   & 70.0 & 1.0 & 4.0 \\
KN   & 70.0 & 1.0 & 4.0 \\
WH   &  4.0 & 0.1 & (0.0) \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Problem Formulation}
% ============================================================

\subsection{State, Observation, and Action}
At time step $t$, the robot state is $x_t = \{q_t, \dot{q}_t\}$,
where $q_t$ and $\dot{q}_t$ are the generalized coordinates
and velocities.

The policy receives a \textbf{41-dimensional} observation vector
\begin{equation}
o_t = \big[
  g_t^b,\;
  v_t^b,\;
  \omega_t^b,\;
  q_{j,t},\;
  \dot{q}_{j,t},\;
  a_{t-1},\;
  \bar{h}_t^{cmd},\;
  e_{\psi,t}
\big] \in \mathbb{R}^{41},
\label{eq:obs}
\end{equation}
where:
\begin{itemize}
    \item $g_t^b \in \mathbb{R}^3$: gravity vector expressed in the
    body frame (from IMU accelerometer);
    \item $v_t^b \in \mathbb{R}^3$: body-frame linear velocity;
    \item $\omega_t^b \in \mathbb{R}^3$: body-frame angular velocity;
    \item $q_{j,t} \in \mathbb{R}^{10}$: joint positions;
    \item $\dot{q}_{j,t} \in \mathbb{R}^{10}$: joint velocities;
    \item $a_{t-1} \in \mathbb{R}^{10}$: previous smoothed policy action;
    \item $\bar{h}_t^{cmd} \in \mathbb{R}$: normalized height command;
    \item $e_{\psi,t} \in \mathbb{R}$: yaw error (heading drift from
    episode start).
\end{itemize}

The base 39-dimensional observation
$[g_t^b,\, v_t^b,\, \omega_t^b,\, q_{j,t},\, \dot{q}_{j,t},\, a_{t-1}]$
is inherited from the base environment.
The two task-specific extensions are:

\noindent\textbf{Normalized height command.}
\begin{equation}
\bar{h}_t^{cmd} = \frac{h_t^{cmd} - h_{\min}}{h_{\max} - h_{\min}},
\label{eq:height_norm}
\end{equation}
where $h_{\min} = \SI{0.40}{m}$ and $h_{\max} = \SI{0.70}{m}$.
The normalization always uses the full task range, so the value is in
$[0, 1]$ regardless of the current curriculum level.

\noindent\textbf{Yaw error.}
\begin{equation}
e_{\psi,t} = \mathrm{wrap}\!\left(\psi_t - \psi_0\right),
\label{eq:yaw_error}
\end{equation}
where $\psi_t$ is the current torso yaw extracted from the base
quaternion via Euler decomposition, $\psi_0$ is the yaw at episode
reset, and $\mathrm{wrap}(\cdot)$ maps to $(-\pi, \pi]$.
This term is necessary because the gravity-body-frame component
$g_t^b$ is yaw-invariant: a memoryless MLP cannot perceive
accumulated heading drift from $g^b$ and $\omega^b$ alone.

When linear velocity is unavailable (e.g., in sim-to-real preparation
without a reliable velocity estimator), $v_t^b$ can be removed,
reducing the observation to 38 dimensions.
All results in this paper use simulator-exact linear velocity,
which corresponds to the upper bound on velocity-estimate quality.

Observations are normalized online using Welford's running mean and
variance estimator~\cite{welford1962}, updated during training
rollouts and held fixed during evaluation.

The policy outputs a normalized action
$a_t = \pi_\theta(o_t) \in [-1,1]^{10}$,
converted to actuator torques through the PID controller
described in Section~\ref{sec:pid}.

\subsection{Learning Objective}
We seek a policy $\pi_\theta$ that maximizes the expected discounted return
\begin{equation}
J(\theta)=\mathbb{E}_{\pi_\theta}\!\left[\sum_{t=0}^{T-1}\gamma^t r_t\right],
\end{equation}
where $\gamma = 0.99$ and $r_t$ is the reward at step $t$.

Training is performed using PPO~\cite{ppo} with clipped surrogate objective
\begin{equation}
L^{\mathrm{PPO}}(\theta) =
\mathbb{E}_t\!\left[
\min\!\left(
\rho_t(\theta)\hat{A}_t,\;
\mathrm{clip}\big(\rho_t(\theta), 1-\epsilon, 1+\epsilon\big)\hat{A}_t
\right)
\right],
\end{equation}
where $\rho_t(\theta)=\pi_\theta(a_t|o_t)/\pi_{\theta_{\mathrm{old}}}(a_t|o_t)$
and $\epsilon=0.2$.
The full objective adds a clipped value loss (coefficient 0.5) and
entropy regularization (coefficient 0.005).
Advantages $\hat{A}_t$ are computed using Generalized Advantage
Estimation (GAE)~\cite{gae} with $\lambda=0.95$.

% ============================================================
\section{Reward Design}
\label{sec:reward}
% ============================================================

\subsection{Reward Structure}
The total reward at each step is a weighted sum:
\begin{equation}
r_t = \sum_i w_i\, r_t^{(i)}.
\label{eq:reward_total}
\end{equation}
The complete set of reward terms and weights for the \texttt{balance}
task are given in Table~\ref{tab:reward_weights}.

\begin{table}[t]
\centering
\caption{Reward components and weights for the variable-height
\texttt{balance} task.
Weights for the \texttt{balance\_robust} fine-tuning stage differ:
$w_{\mathrm{still}}=0$, $w_{\mathrm{wh}}=0$,
$w_{\mathrm{nat}}=1.5$, $w_{\mathrm{alive}}=0.5$,
$w_{\Delta a}=-0.005$, to accommodate wheel-based push recovery motion.}
\label{tab:reward_weights}
\begin{tabular}{l l r}
\toprule
Component & Type & Weight \\
\midrule
Height tracking ($r_{\mathrm{height}}$)    & Positive & 2.5    \\
Body level ($r_{\mathrm{level}}$)          & Positive & 1.5    \\
Position drift ($r_{\mathrm{drift}}$)      & Positive & 1.5    \\
Orientation ($r_{\mathrm{orient}}$)        & Positive & 0.8    \\
Symmetry ($r_{\mathrm{sym}}$)              & Positive & 1.0    \\
Heading ($r_{\mathrm{heading}}$)           & Positive & 0.5    \\
Legs forward ($r_{\mathrm{fwd}}$)          & Positive & 0.5    \\
Legs vertical ($r_{\mathrm{vert}}$)        & Positive & 0.5    \\
No motion ($r_{\mathrm{still}}$)           & Positive & 0.5    \\
Yaw suppression ($r_{\mathrm{yaw}}$)       & Positive & 0.5    \\
Natural pose ($r_{\mathrm{nat}}$)          & Positive & 0.4    \\
Alive bonus ($r_{\mathrm{alive}}$)         & Positive & 0.3    \\
Action rate ($p_{\Delta a}$)               & Penalty  & $-0.05$    \\
Wheel velocity ($p_{\mathrm{wh}}$)         & Penalty  & $-0.005$   \\
Joint torque ($p_\tau$)                    & Penalty  & $-0.0005$  \\
Joint velocity ($p_{\dot{q}}$)             & Penalty  & $-0.00061$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Primary Reward Terms}

\subsubsection{Body-Level Reward}
To encourage the torso to remain parallel to the ground:
\begin{equation}
r_{\mathrm{level}} =
\exp\!\left(-\frac{\phi_t^2}{\sigma_r^2}\right)
\exp\!\left(-\frac{\theta_t^2}{\sigma_p^2}\right),
\label{eq:body_level}
\end{equation}
where $\phi_t$ and $\theta_t$ are the torso roll and pitch angles,
and $\sigma_r = \sigma_p = 0.15\,\mathrm{rad}$.
The threshold $\sigma = 0.15\,\mathrm{rad}$ ($\approx8.6^\circ$) corresponds
to the boundary of stable balance for a wheeled inverted pendulum at the
nominal height: beyond this lean angle the TWIP linearization becomes
inaccurate and recovery is uncertain.
This choice ensures the reward remains near 1.0 during upright stance
and falls sharply only when tilt enters the unstable regime,
providing a tight but achievable target for the policy.

\subsubsection{Height Tracking Reward}
\begin{equation}
r_{\mathrm{height}} =
\exp\!\left(
-\frac{(h_t - h_t^{cmd})^2}{\sigma_h^2}
\right),
\label{eq:height_reward}
\end{equation}
with $\sigma_h = 0.25\,\mathrm{m}$.
This bandwidth was selected to provide a non-vanishing gradient signal
across the full commanded height range $[0.40, 0.70]\,\mathrm{m}$:
at the midpoint of a 0.15\,m tracking error, the reward retains
$\exp(-0.15^2/0.25^2) \approx 0.83$ of its maximum value,
ensuring a smooth gradient that guides learning even during early training
when height tracking is poor.
A tighter $\sigma$ would produce near-zero gradients for heights far from
the target and destabilize early-stage curriculum learning.

\subsubsection{Heading and Position Regularization}
To keep the robot near its initial yaw $\psi_0$ and anchor
position $p_{xy,0}$:
\begin{equation}
r_{\mathrm{heading}} =
\exp\!\left(
-\frac{\mathrm{wrap}(\psi_t - \psi_0)^2}{\sigma_\psi^2}
\right),
\label{eq:heading_reward}
\end{equation}
\begin{equation}
r_{\mathrm{drift}} =
\exp\!\left(
-\frac{\|p_{xy,t} - p_{xy,0}\|_2^2}{\sigma_d^2}
\right),
\label{eq:drift_reward}
\end{equation}
with $\sigma_\psi = 0.5\,\mathrm{rad}$ and $\sigma_d = 0.5\,\mathrm{m}$.

\subsubsection{Orientation and Yaw Stability}
Linear orientation stability (avoids saturation at high angular rates):
\begin{equation}
r_{\mathrm{orient}} =
\mathrm{clip}\!\left(1 - 0.15\,\|\omega_t^b\|_2,\; 0,\; 1\right),
\label{eq:orient_reward}
\end{equation}
and a dedicated yaw suppression term:
\begin{equation}
r_{\mathrm{yaw}} =
\mathrm{clip}\!\left(1 - 0.5\,|\omega_{z,t}^b|,\; 0,\; 1\right).
\label{eq:yaw_reward}
\end{equation}

\subsubsection{Leg Symmetry}
\begin{equation}
r_{\mathrm{sym}} =
\exp\!\left(
-\frac{
\left\|
q_{\mathrm{leg},t}^L - q_{\mathrm{leg},t}^R
\right\|_2^2
}{\sigma_{\mathrm{sym}}^2}
\right),
\label{eq:symmetry_reward}
\end{equation}
where $q_{\mathrm{leg}}^{L/R}$ includes hip roll, hip yaw, hip pitch,
and knee for each leg, and $\sigma_{\mathrm{sym}} = 0.5$.

\subsubsection{Leg Alignment Rewards}
Two terms encourage well-aligned leg orientation:

\noindent\textbf{Legs forward} ($r_{\mathrm{fwd}}$):
rewards hip yaw close to zero (feet pointing forward):
\begin{equation}
r_{\mathrm{fwd}} =
\exp\!\left(-\frac{(q_{\mathrm{hy}}^L)^2}{\sigma_{\ell}^2}\right)
\exp\!\left(-\frac{(q_{\mathrm{hy}}^R)^2}{\sigma_{\ell}^2}\right).
\label{eq:legs_forward}
\end{equation}

\noindent\textbf{Legs vertical} ($r_{\mathrm{vert}}$):
rewards hip roll close to zero (legs perpendicular to the ground plane):
\begin{equation}
r_{\mathrm{vert}} =
\exp\!\left(-\frac{(q_{\mathrm{hr}}^L)^2}{\sigma_{\ell}^2}\right)
\exp\!\left(-\frac{(q_{\mathrm{hr}}^R)^2}{\sigma_{\ell}^2}\right),
\label{eq:legs_vertical}
\end{equation}
where $\sigma_{\ell} = 0.15\,\mathrm{rad}$ for both terms.

\subsubsection{No-Motion Reward}
To encourage stationary balance:
\begin{equation}
r_{\mathrm{still}} =
\exp\!\left(
-\frac{\|v_t^b\|_2^2}{\sigma_v^2}
\right),
\label{eq:no_motion}
\end{equation}
with $\sigma_v = 0.2\,\mathrm{m/s}$.
This term is set to zero in the \texttt{balance\_robust} stage,
where wheel motion is needed for push recovery.

\subsection{Height-Consistent Natural Pose Reward}
\label{sec:natural_pose}
A key design of our method is a posture regularization term that enforces
consistency between the commanded torso height and the desired leg
configuration.
Let the desired hip-pitch and knee angles
$q_{\mathrm{hp}}^*(h^{cmd})$ and $q_{\mathrm{kn}}^*(h^{cmd})$
be computed by piecewise-linear interpolation.

The kinematic keypoints, derived from forward kinematics of the two-link
leg model, are:
\begin{align*}
h = 0.72\,\mathrm{m}\text{:} &\quad q_{\mathrm{hp}}^* = 0.0,\quad q_{\mathrm{kn}}^* = 0.0 \\
h = 0.71\,\mathrm{m}\text{:} &\quad q_{\mathrm{hp}}^* = 0.3,\quad q_{\mathrm{kn}}^* = 0.5 \\
h = 0.38\,\mathrm{m}\text{:} &\quad q_{\mathrm{hp}}^* = 1.5,\quad q_{\mathrm{kn}}^* = 2.5
\end{align*}

For the high-standing segment
$h^{cmd} \in [0.71, 0.72]\,\mathrm{m}$:
\begin{equation}
t_h = \frac{h^{cmd} - 0.71}{0.01},
\quad
q_{\mathrm{hp}}^* = 0.3(1 - t_h),
\quad
q_{\mathrm{kn}}^* = 0.5(1 - t_h).
\end{equation}

For the lower-height segment $h^{cmd} \in [0.38, 0.71]\,\mathrm{m}$
(which covers the full \texttt{balance} training range):
\begin{equation}
t_l = \frac{h^{cmd} - 0.38}{0.33},
\quad
q_{\mathrm{hp}}^* = 1.5 - 1.2\,t_l,
\quad
q_{\mathrm{kn}}^* = 2.5 - 2.0\,t_l.
\end{equation}

The posture error is
\begin{equation}
e_{\mathrm{pose}} =
\Big(
(q_{\mathrm{hp}}^L - q_{\mathrm{hp}}^*)^2
+
(q_{\mathrm{hp}}^R - q_{\mathrm{hp}}^*)^2
+
(q_{\mathrm{kn}}^L - q_{\mathrm{kn}}^*)^2
+
(q_{\mathrm{kn}}^R - q_{\mathrm{kn}}^*)^2
\Big)^{1/2}.
\end{equation}

The natural pose reward is
\begin{equation}
r_{\mathrm{nat}} =
\exp\!\left(
-\frac{e_{\mathrm{pose}}^2}{\sigma_{nat}^2}
\right)
r_{\mathrm{knee}},
\label{eq:natural_pose}
\end{equation}
where $\sigma_{nat} = 0.8\,\mathrm{rad}$ and $r_{\mathrm{knee}}$
penalizes knee hyperextension:
\begin{equation}
r_{\mathrm{knee}}=
\exp\!\left(-\frac{\min(q_{\mathrm{kn}}^L,0)^2}{\sigma_k^2}\right)
\exp\!\left(-\frac{\min(q_{\mathrm{kn}}^R,0)^2}{\sigma_k^2}\right),
\end{equation}
with $\sigma_k = 0.15\,\mathrm{rad}$.

This reward prevents the policy from exploiting unnatural joint
configurations (e.g., extreme hip abduction with nearly straight knees)
to satisfy a height target without a kinematically meaningful posture.

\noindent\textbf{Design rationale.}
Without this term, a height-tracking reward alone permits degenerate
solutions: the robot can achieve the target CoM height by bending
one leg more than the other (asymmetric collapse), hyperextending
the knee, or adopting an extreme hip-pitch that satisfies the height
constraint but produces an unstable inverted-pendulum configuration.
The piecewise-linear interpolation maps each commanded height to a
physically meaningful (hip-pitch, knee) pair derived from the
two-link leg geometry, effectively constraining the null space of the
height objective to stable, symmetric postures.
The soft exponential penalty ($\sigma_{\mathrm{nat}} = 0.5\,\mathrm{rad}$)
allows exploration during early training while strongly penalizing
configurations that deviate more than $\approx 0.5\,\mathrm{rad}$
from the target posture.

\subsection{Effort and Smoothness Penalties}
We regularize torque magnitude, joint velocity, action variation,
and wheel rotation:
\begin{equation}
p_\tau = \sum_{j=1}^{10} \tau_{t,j}^2,
\quad
p_{\dot{q}} = \sum_{j=1}^{10} \dot{q}_{j,t}^2,
\quad
p_{\Delta a} = \sum_{j=1}^{10}(a_{t,j} - a_{t-1,j})^2,
\end{equation}
\begin{equation}
p_{\mathrm{wh}} = \dot{q}_{\mathrm{wh},t}^{L\,2} + \dot{q}_{\mathrm{wh},t}^{R\,2}.
\end{equation}
The wheel velocity penalty and $r_{\mathrm{still}}$ together enforce
stationary balance in the \texttt{balance} stage;
both are set to zero in \texttt{balance\_robust}.

\subsection{Termination Conditions}
An episode terminates early if the torso tilt exceeds $0.8\,\mathrm{rad}$
($\approx 46^\circ$) or the torso height drops below $0.3\,\mathrm{m}$.
Otherwise, episodes run for 1000 steps ($\approx\SI{20}{s}$).
A survival bonus $r_{\mathrm{alive}} \in \{0, 1\}$ is given at each
non-terminal step, providing a constant per-step incentive to remain standing.

% ============================================================
\section{Curriculum Learning Strategy}
\label{sec:curriculum}
% ============================================================

\subsection{Within-Stage Height Curriculum}
Rather than sampling the commanded height from the full feasible range
from the beginning, we employ a within-stage curriculum~\cite{bengio2009curriculum}
that progressively expands the lower bound of the height command distribution.

The height lower bound is decreased by a fixed step $\delta_h$ whenever
performance exceeds a threshold:
\begin{equation}
h_{\min}^{(k+1)} = \max\!\big(h_{\min}^{(k)} - \delta_h,\;
h_{\min}^{\mathrm{final}}\big),
\quad
\text{if } \bar{r}_{\mathrm{eval}} \geq r_{\mathrm{thr}}.
\label{eq:height_curriculum}
\end{equation}

\noindent\textbf{Advancement signal.}
Every $I_{\mathrm{eval}} = 2$ training updates, the current policy is
evaluated greedily (no exploration noise) over 200 complete episodes
using 32 parallel environments, with the observation normalizer held fixed.
The per-step return is
\begin{equation}
\bar{r}_{\mathrm{eval}} = \frac{R_{\mathrm{mean}}}{T},
\label{eq:eval_per_step}
\end{equation}
where $R_{\mathrm{mean}}$ is the mean episode return
and $T = 1000$ is the episode length.
Advancement fires when $\bar{r}_{\mathrm{eval}} \geq r_{\mathrm{thr}} = 0.75$.
Using a greedy evaluation pass rather than the noisy per-update training
reward produces a more reliable signal for curriculum decisions.

\noindent\textbf{Curriculum parameters.}
Initial lower bound: $h_{\min}^{\mathrm{init}} = \SI{0.69}{m}$;
final lower bound: $h_{\min}^{\mathrm{final}} = \SI{0.40}{m}$;
$N_c = 29$ discrete levels;
$\delta_h \approx \SI{0.01}{m}$;
threshold $r_{\mathrm{thr}} = 0.75$.
At each level $k$:
\begin{equation}
h_t^{cmd} \sim \mathcal{U}\!\big(h_{\min}^{(k)},\; h_{\max}\big),
\end{equation}
where $h_{\max} = \SI{0.70}{m}$.

The 29 levels span three qualitative phases:
\begin{itemize}
    \item \textbf{Phase A} (levels 1--5):
    $h_{\min} \in [0.65, 0.69]\,\mathrm{m}$;
    narrow band near nominal; policy focuses on stable upright stance.
    \item \textbf{Phase B} (levels 6--20):
    $h_{\min} \in [0.50, 0.64]\,\mathrm{m}$;
    widening toward moderate crouching.
    \item \textbf{Phase C} (levels 21--29):
    $h_{\min} \in [0.40, 0.49]\,\mathrm{m}$;
    full operating range including deep crouching.
\end{itemize}

% [TODO-FIGURE: curriculum progression figure showing minimum commanded height
%  and per-step evaluation reward vs.\ training updates,
%  with Phase A/B/C boundaries annotated. Three-seed mean ± std preferred.]

\subsection{Multi-Stage Macro-Curriculum}
\label{sec:multi_stage}
A multi-stage curriculum manager chains training stages with
warm-starting and performance-gated promotion and demotion.
The three stages are:

\begin{enumerate}
    \item \textbf{\texttt{balance}} (this paper):
    variable-height stationary balance with within-stage height curriculum
    as described above.
    No external push disturbances ($F_{\mathrm{push}} = 0$).
    Macro-curriculum promotion threshold: 80\% success rate with
    $w_{\mathrm{eval}} = 7.0\,\mathrm{reward/step}$.

    \item \textbf{\texttt{stand\_up}}:
    height transitions and recovery from non-nominal initial poses.
    Initialized (warm-started) from the \texttt{balance} checkpoint.
    Promotion threshold: 80\% success rate with
    $w_{\mathrm{eval}} = 5.0\,\mathrm{reward/step}$.
    % [TODO-CLAIM: stand_up stage has environment and config implemented
    %  but has not been trained or evaluated at time of writing;
    %  no performance claims are made.]

    \item \textbf{\texttt{balance\_robust}}:
    fine-tuning under periodic random push disturbances
    ($F_{\mathrm{push}} = \SI{40}{N}$, applied over 8 control steps,
    every 200 steps $\approx\SI{4}{s}$).
    Initialized from the \texttt{stand\_up} checkpoint.
    Reward weights modified: $w_{\mathrm{still}} = w_{\mathrm{wh}} = 0$;
    $w_{\mathrm{nat}}$ increased to 1.5;
    $w_{\Delta a}$ reduced to $-0.005$.
    Promotion threshold: 80\% success rate with
    $w_{\mathrm{eval}} = 6.0\,\mathrm{reward/step}$.
    % [TODO-CLAIM: balance_robust stage has environment and config
    %  implemented but has not been trained or evaluated;
    %  no performance claims are made.]
\end{enumerate}

Demotion fires when success rate falls below 30\% within a window
of 100 evaluation episodes.
Stages \texttt{stand\_up} and \texttt{balance\_robust} have corresponding
environment implementations and training configurations
but have not yet been trained; their results are not reported here.

% ============================================================
\section{Classical LQR Baseline}
\label{sec:lqr}
% ============================================================

\subsection{Motivation}
Classical control methods provide an interpretable reference
for evaluating RL policies.
We implement an LQR baseline that operates on
the same 41-dimensional observation interface and shares the same
PID low-level controller as the RL policy, enabling a
controlled comparison without confounding actuator-interface differences.

\subsection{Linearized TWIP Model}
The sagittal balance component is based on a linearized
two-wheeled inverted pendulum (TWIP) model.
The state and input are
\begin{equation}
x_{\mathrm{lqr}} =
\begin{bmatrix}
\theta_{\mathrm{pitch}} \\
\dot{\theta}_{\mathrm{pitch}} \\
v_{\mathrm{fwd}} \\
p_{\mathrm{fwd}}
\end{bmatrix},
\quad
u = \bar{\omega}_{\mathrm{wheel}},
\end{equation}
where $\theta_{\mathrm{pitch}}$ is the forward lean angle,
$v_{\mathrm{fwd}}$ is the forward body velocity,
$p_{\mathrm{fwd}}$ is the forward position drift,
and $u$ is the average wheel velocity command.

The continuous-time linearization
$\dot{x}_{\mathrm{lqr}} = A\,x_{\mathrm{lqr}} + B\,u$
uses robot constants:
mass $m = \SI{8.1}{kg}$,
nominal CoM height $l_c = \SI{0.54}{m}$ (updated per height command via IK),
and wheel radius $r_w = \SI{0.06}{m}$.

The LQR gain is solved from the continuous algebraic Riccati equation
(CARE) with state cost $Q = \mathrm{diag}(10, 2, 3, 0.3)$
and input cost $R = 0.8$:
\begin{equation}
K_{\mathrm{lqr}} = R^{-1} B^\top P,
\quad
A^\top P + P A - P B R^{-1} B^\top P + Q = 0.
\end{equation}

\subsection{Height-Dependent Inverse Kinematics}
To extend the LQR to variable commanded heights, we perform an offline IK
scan over hip-pitch angles from $0$ to $1.35\,\mathrm{rad}$, with the
knee set to $q_{\mathrm{kn}} = 2\,q_{\mathrm{hp}}$ (symmetric two-link
fold geometry).
The resulting height--angle mapping is fit by a degree-2 polynomial,
yielding target joint angles for any commanded height in $[0.40, 0.70]\,\mathrm{m}$.
The effective CoM height $l_c(h^{cmd})$ fed into the TWIP linearization
is updated accordingly.

\subsection{Multi-Axis Control}
Lateral and yaw axes are controlled by independent PD terms:
\begin{align}
u_{\mathrm{lat}} &=
K_{p,\mathrm{roll}} \cdot e_{\mathrm{roll}}
+ K_{d,\mathrm{roll}} \cdot \dot{e}_{\mathrm{roll}}, \\
u_{\mathrm{yaw}} &=
K_{p,\mathrm{yaw}} \cdot e_{\psi}
+ K_{d,\mathrm{yaw}} \cdot \omega_z,
\end{align}
with $K_{p,\mathrm{roll}} = 0.4$, $K_{d,\mathrm{roll}} = 0.08$,
$K_{p,\mathrm{yaw}} = 2.5$, $K_{d,\mathrm{yaw}} = 0.2$.
The final wheel command is the superposition of sagittal LQR,
lateral PD, and yaw PD outputs, clipped to $\pm\SI{20.0}{rad/s}$.

\subsection{Assumptions and Limitations}
The LQR baseline has the following known limitations:
\begin{enumerate}
    \item \textbf{Linear regime only.}
    The TWIP linearization is valid near the nominal standing height
    and small lean angles ($|\theta_{\mathrm{pitch}}| \lesssim 10^\circ$).
    At large lean deviations or very low commanded heights, accuracy degrades.
    \item \textbf{No lean integral.}
    Without an integral term on lean error, constant disturbances
    (friction variation, slopes) produce nonzero steady-state lean.
    \item \textbf{No large-disturbance recovery.}
    Under strong pushes the state leaves the linear regime;
    the LQR provides no recovery mechanism.
    \item \textbf{Decoupled axes.}
    Sagittal, lateral, and yaw dynamics are treated independently,
    ignoring cross-coupling during aggressive maneuvers.
\end{enumerate}

The LQR baseline is therefore a fair reference for the \texttt{nominal}
and \texttt{narrow\_height} scenarios but is expected to degrade at
full height range, strong pushes, and low friction.
% [TODO-CLAIM: degradation of LQR under push/full-range must be
%  verified experimentally before being stated as a confirmed result.]

% ============================================================
\section{Training Pipeline and Evaluation Protocol}
% ============================================================

\subsection{Network Architecture}
Separate MLPs for policy and value function: hidden sizes
$\{256, 256, 128\}$, ELU activation, orthogonal weight initialization,
implemented in Flax~\cite{flax}.
The policy outputs the mean of a Gaussian action distribution with a
learned log-standard-deviation initialized to $\log(0.2)$.

\subsection{Training Configuration}
PPO with 4096 parallel environments, rollout length 128 steps,
4 epochs per update, 32 minibatches, learning rate
$2\!\times\!10^{-4}$ (Adam), gradient norm clipping 0.5,
entropy coefficient 0.005, value-loss coefficient 0.5.
% [TODO-RESULT: report actual total training steps once complete;
%  planned target is approximately 50M environment steps]

The complete set of hyperparameters is summarized in
Table~\ref{tab:ppo_hyperparams} for reproducibility.

\begin{table}[t]
\centering
\caption{PPO training hyperparameters for the \texttt{balance} stage.}
\label{tab:ppo_hyperparams}
\begin{tabular}{l r}
\toprule
Hyperparameter & Value \\
\midrule
\multicolumn{2}{l}{\textit{Environment}} \\
Parallel environments    & 4{,}096 \\
Rollout length           & 128 steps \\
Control frequency        & 50\,Hz (20\,ms/step) \\
Episode length           & 1{,}000 steps (20\,s) \\
\midrule
\multicolumn{2}{l}{\textit{PPO Update}} \\
Epochs per update        & 4 \\
Minibatches per epoch    & 32 \\
Minibatch size           & 16{,}384 \\
PPO clip range ($\epsilon$) & 0.2 \\
\midrule
\multicolumn{2}{l}{\textit{Optimization}} \\
Optimizer                & Adam \\
Learning rate            & $2\times10^{-4}$ \\
Gradient norm clip       & 0.5 \\
Value loss coefficient   & 0.5 \\
Entropy coefficient      & 0.005 \\
\midrule
\multicolumn{2}{l}{\textit{Advantage Estimation}} \\
Discount $\gamma$        & 0.99 \\
GAE $\lambda$            & 0.95 \\
\midrule
\multicolumn{2}{l}{\textit{Network}} \\
Hidden layers (actor)    & $\{256, 256, 128\}$ \\
Hidden layers (critic)   & $\{256, 256, 128\}$ \\
Activation               & ELU \\
Weight init              & Orthogonal \\
Initial log-std          & $\log(0.2)$ \\
\midrule
\multicolumn{2}{l}{\textit{Curriculum Evaluation}} \\
Eval interval            & every 2 updates \\
Eval environments        & 32 \\
Eval episodes            & 200 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Research Evaluation Protocol}
\label{sec:eval_protocol}
Research evaluation is performed offline using a deterministic
single-environment MuJoCo rollout with per-step telemetry recording.
This evaluation pipeline is strictly separate from the in-training
curriculum advancement evaluation, which uses a vectorized greedy
rollout over the current policy to gate height-range expansion.

\noindent\textbf{Evaluation scenarios.}
Ten scenario groups are defined:

\begin{enumerate}
    \item \textbf{Nominal}: flat floor, $h^{cmd} = \SI{0.65}{m}$,
    standard domain randomization.
    \item \textbf{Narrow height}: $h^{cmd} = \SI{0.69}{m}$
    (Phase A, near-nominal).
    \item \textbf{Wide height}: $h^{cmd} = \SI{0.60}{m}$
    (Phase B, moderate crouching).
    \item \textbf{Full range}: $h^{cmd} \sim \mathcal{U}[0.40, 0.70]$
    (Phase C, full range).
    \item \textbf{Push recovery}: single $\SI{50}{N}$ impulse at
    $t = \SI{3}{s}$; measures recovery time and maximum recoverable
    disturbance via binary search
    (range \SIrange{10}{300}{N}, 8 iterations, 50\% survival threshold).
    \item \textbf{Friction low}: friction multiplier $0.6\times$.
    \item \textbf{Friction high}: friction multiplier $1.4\times$.
    \item \textbf{Sensor noise + delay}: IMU/encoder noise plus
    1-step ($\approx\SI{20}{ms}$) action delay.
    \item \textbf{Push sweep}: parametric sweep over
    $\{20, 40, 60, 80, 100, 130, 160, 200\}\,\mathrm{N}$
    for degradation curves.
    \item \textbf{Friction sweep}: parametric sweep over
    friction multipliers $\{0.3, 0.5, 0.7, 1.0, 1.3, 1.8\}$.
\end{enumerate}

\noindent\textbf{Evaluation metrics.}
Ten metrics are computed per scenario:
\begin{enumerate}
    \item Survival time [s]
    \item Pitch RMS [deg]
    \item Roll RMS [deg]
    \item Pitch-rate RMS [rad/s]
    \item XY drift max [m]
    \item Height RMSE [m]
    \item Wheel speed RMS [rad/s]
    \item Torque RMS [N$\cdot$m]
    \item Recovery time [s] (push\_recovery only; NaN otherwise)
    \item Max recoverable push [N] (push\_recovery only)
\end{enumerate}

Aggregate metrics: fall rate, survival rate.

\noindent\textbf{Multi-seed evaluation protocol.}
Final RL results are reported as mean $\pm$ standard deviation over three
independent training runs initialized with random seeds 42, 113, and 999.
Each run is trained from scratch with a distinct random state;
no parameters, replay data, or normalization statistics are shared between
runs during training or evaluation.
The offline evaluation pipeline is applied identically to all three
trained policies, and scalar metrics are aggregated post-hoc.
The LQR baseline requires no training and is evaluated once under the
same scenario suite.

% [TODO-TABLE: RL vs LQR comparison table. Columns: scenario (nominal,
%  narrow_height, full_range, push_recovery, friction_low, friction_high).
%  Rows: RL policy and LQR baseline.
%  Report: survival_rate, height_rmse_m, pitch_rms_deg,
%  torque_rms_nm, max_recoverable_push_n.]
\begin{table}[t]
\centering
\caption{RL policy vs.\ LQR baseline comparison (planned).
All result cells to be filled after training.}
\label{tab:rl_vs_lqr}
\begin{tabular}{@{}llccccc@{}}
\toprule
& Scenario & Surv.\ & $e_h$ & Pitch & Torque & Push \\
& & (\%) & (m) & (deg) & (Nm) & (N) \\
\midrule
\multirow{5}{*}{RL}
& Nominal       & [TODO] & [TODO] & [TODO] & [TODO] & [TODO] \\
& Narrow        & [TODO] & [TODO] & [TODO] & [TODO] & --- \\
& Full range    & [TODO] & [TODO] & [TODO] & [TODO] & --- \\
& Push recov.   & [TODO] & [TODO] & [TODO] & [TODO] & [TODO] \\
& Friction low  & [TODO] & [TODO] & [TODO] & [TODO] & --- \\
\midrule
\multirow{5}{*}{LQR}
& Nominal       & [TODO] & [TODO] & [TODO] & [TODO] & [TODO] \\
& Narrow        & [TODO] & [TODO] & [TODO] & [TODO] & --- \\
& Full range    & [TODO] & [TODO] & [TODO] & [TODO] & --- \\
& Push recov.   & [TODO] & [TODO] & [TODO] & [TODO] & [TODO] \\
& Friction low  & [TODO] & [TODO] & [TODO] & [TODO] & --- \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Experimental Results}
\label{sec:results}
% ============================================================

\textit{Training has not yet been completed.
This section will be populated with quantitative results after the
\texttt{balance} policy is fully trained and evaluated.
All placeholders below indicate planned experiments.}

\subsection{Learning Curves}
% [TODO-FIGURE: training curves showing mean episode return,
%  per-step evaluation reward, and minimum commanded height vs.\ training
%  steps (three seeds, with shaded variance). Mark Phase A/B/C boundaries
%  on the x-axis. Also show eval fall rate and survival rate.]
\textit{[TODO-FIGURE: training curve and curriculum level progression]}

\subsection{Nominal Balance and Variable-Height Results}

\begin{table}[ht]
\centering
\caption{Variable-height balancing: performance vs.\ commanded height.
All values pending training completion.}
\label{tab:balance_results}
\begin{tabular}{ccccc}
\toprule
$h^{cmd}$ (m) & Surv.\ (\%) & $e_h$ (m) & Pitch (deg) & Torque (Nm) \\
\midrule
0.40 & [TODO] & [TODO] & [TODO] & [TODO] \\
0.50 & [TODO] & [TODO] & [TODO] & [TODO] \\
0.60 & [TODO] & [TODO] & [TODO] & [TODO] \\
0.65 & [TODO] & [TODO] & [TODO] & [TODO] \\
0.69 & [TODO] & [TODO] & [TODO] & [TODO] \\
\bottomrule
\end{tabular}
\end{table}
% [TODO-RESULT: insert variable-height benchmark metrics aggregated over
%  3 independent seeds (seeds 42, 113, 999), scenarios: nominal,
%  narrow_height, wide_height, full_range; 50 episodes per scenario.
%  Report mean ± std for survival rate, height RMSE, pitch RMS, torque RMS.]

% [TODO-FIGURE: robot posture snapshots at commanded heights
%  0.40, 0.55, 0.65, 0.69 m showing variable-height balancing]

\subsection{Push Robustness}
% [TODO-FIGURE: push-sweep degradation curve: survival rate vs.\ push magnitude
%  (8 points, 20--200 N), comparing RL policy (3-seed mean ± std) and LQR
%  baseline. Indicate the maximum recoverable push force for each controller.]
% [TODO-RESULT: max_recoverable_push_n for RL policy vs. LQR baseline
%  from push_recovery scenario]
\textit{[TODO-FIGURE: push\_sweep degradation curve;
TODO-RESULT: max recoverable push force]}

\subsection{Friction Robustness}
% [TODO-FIGURE: friction-sweep degradation curve: survival rate vs.\ friction
%  multiplier (6 points, 0.3×--1.8×), comparing RL policy (3-seed mean ± std)
%  and LQR baseline.]
\textit{[TODO-FIGURE: friction\_sweep degradation curve]}

\subsection{Ablation: Natural Pose Reward}
% [TODO-ABLATION: compare full model (natural pose weight 0.4) against
%  a variant with natural pose reward disabled. Report height RMSE and
%  pitch RMS at the deepest commanded height (h=0.40 m), where posture
%  degeneracy is most likely. Also report convergence speed (steps to
%  reach the curriculum advancement threshold). This ablation directly
%  validates the primary methodological contribution.]
\textit{[TODO-ABLATION: natural pose reward ($r_{\mathrm{nat}}$)
ablation: weight=0.4 vs.\ weight=0]}

\subsection{Ablation: Observation Design}
% [TODO-ABLATION: compare policies trained with simulator-exact velocity,
%  noisy velocity, and no velocity in the observation, to assess
%  sensitivity to the quality of the linear velocity estimate.
%  Only report if experiment is conducted.]
\textit{[TODO-ABLATION: velocity-estimate robustness (exact / noisy / absent)
--- only report if experiment is conducted]}

% ============================================================
\section{Discussion and Limitations}
\label{sec:discussion}
% ============================================================

\subsection{Framework Design}
The main design principle is to decompose the variable-height balancing
problem into a progressive curriculum that stabilizes learning by starting
near the nominal standing pose and expanding toward more challenging
configurations.
The height-consistent natural pose reward constrains the joint-space
solution to physically meaningful postures at each target height.

The PID low-level controller decouples the RL policy from actuator-level
dynamics and is shared with the LQR baseline, ensuring that observed
performance differences reflect controller intelligence rather than
interface differences.

The yaw-error observation term $e_{\psi,t}$ closes the heading loop
that gravity-body-frame observations alone cannot.
Without it, a memoryless MLP requires undesirable implicit integration of
angular velocity over many steps to sense accumulated yaw drift.

\subsection{Sim-to-Real Considerations}
\label{sec:sim2real}
Although the present work is conducted entirely in simulation,
the framework is designed with hardware transfer in mind.

\noindent\textbf{Action representation.}
The policy outputs PD position targets rather than raw torques.
This action representation has been shown to transfer reliably across
the sim-to-real gap for legged locomotion~\cite{hwangbo2019learning,rudin2022walk},
as it implicitly low-pass filters the control signal and decouples the policy
from precise actuator gain tuning.

\noindent\textbf{Domain randomization coverage.}
The randomization ranges in Table~\ref{tab:domain_rand} were chosen to
bracket the expected real-world variability: friction variation covers
floor surfaces from polished tile (low) to rubber mat (high),
mass perturbation covers battery charge variation and light payloads,
and action delay covers the communication latency of the embedded
motor controller.

\noindent\textbf{Observation availability.}
The policy uses body-frame linear velocity, which requires a velocity
estimator on hardware.
Should a reliable estimator be unavailable, the velocity channel can
be dropped, reducing the observation to 38 dimensions.
The policy can be retrained under this reduced observation for
direct deployment, at a modest cost in height-tracking precision.

\noindent\textbf{Transfer plan.}
Planned hardware deployment will proceed in three stages:
(i)~system identification of actuator friction and rotor inertia
to refine the simulator;
(ii)~policy deployment at fixed nominal height ($h^{cmd}=0.65$\,m)
with a fall-safe watchdog;
(iii)~progressive extension to variable-height commands
following the same Phase~A/B/C ordering as training.

\subsection{Current Limitations}
\begin{enumerate}
    \item \textbf{Balance only (experimentally).}
    The experimentally mature component is the \texttt{balance} stage.
    Stages \texttt{stand\_up} and \texttt{balance\_robust} have code
    and configurations but no training results at the time of writing.
    All claims about push recovery and height transitions are structural,
    not performance-validated.

    \item \textbf{No quantitative results yet.}
    Training runs have not been completed.
    All result sections contain placeholders.
    Claims in this paper concern framework design, not demonstrated
    performance.

    \item \textbf{No sim-to-real transfer (yet).}
    All work is conducted in simulation.
    Although the robot model matches a physical platform,
    sim-to-real transfer has not been attempted.
    The sensor-noise and action-delay configurations are designed for
    sim-to-real preparation following established protocols~%
    \cite{tobin2017domain,peng2018simtoreal,tan2018simtoreal},
    but hardware validation remains as future work
    (see Section~\ref{sec:sim2real}).

    \item \textbf{Multi-seed training planned, not yet complete.}
    Final reported metrics are to be aggregated over three independent
    training runs (seeds 42, 113, and 999), each trained from scratch.
    Quantitative results will report mean $\pm$ std across all three seeds.
    % [TODO-CLAIM: report multi-seed variance before asserting
    %  statistical significance of any comparison; all three seeds
    %  must complete training before filling result tables]

    \item \textbf{LQR at large disturbances.}
    The LQR is only valid near the nominal height and small lean angles.
    Comparisons at full range, strong pushes, and low friction are
    expected to favor RL, but this must be confirmed experimentally.
    % [TODO-CLAIM: only state LQR inferiority at large disturbances after
    %  push_recovery and full_range scenarios are evaluated]

    \item \textbf{Curriculum advancement signal noise.}
    The within-stage height curriculum fires every 2 training updates
    using 32 envs $\times$ 200 episodes.
    Early-training evaluations may produce noisy advancement signals
    before the policy stabilizes.
\end{enumerate}

% ============================================================
\section{Conclusion}
% ============================================================
This paper presented a curriculum reinforcement learning framework for
variable-height balancing of a 10-actuated wheeled biped robot.
The core technical contributions are: (i)~a height-conditioned policy
trained via PPO in GPU-accelerated MuJoCo MJX simulation with 4,096
parallel environments; (ii)~a within-stage height curriculum that
expands the commanded height range from near-nominal standing to
deep crouching across 29 levels, gated by a greedy evaluation pass;
(iii)~a height-consistent natural pose reward that maps commanded
height to target joint angles via piecewise-linear interpolation,
preventing degenerate balancing strategies; and (iv)~a classical LQR
baseline sharing the same PID actuator interface, enabling controlled
comparison across a suite of ten evaluation scenarios.

The framework is designed for sim-to-real transfer: PD position targets
as the action representation, comprehensive domain randomization covering
friction, mass, motor strength, and sensor noise, and an observation
space that degrades gracefully when linear velocity is unavailable.
A multi-stage macro-curriculum (\texttt{balance} $\to$
\texttt{stand\_up} $\to$ \texttt{balance\_robust}) provides a path
toward robust recovery from falls and active height transitions.

% [TODO-RESULT: Once training is complete, replace the following sentence
%  with a summary of key quantitative findings, e.g.:
%  "Experimental results show that the RL policy achieves X m height RMSE
%   across the full [0.40, 0.70] m range and recovers from pushes up to Y N,
%   outperforming the LQR baseline on all full-range and disturbance metrics."]
Experimental evaluation and quantitative comparison with the LQR baseline
are ongoing; results will be reported in the final version.

Future work will focus on hardware deployment via the staged transfer
plan described in Section~\ref{sec:sim2real}, extension of the
macro-curriculum to the \texttt{stand\_up} and \texttt{balance\_robust}
stages, and integration of exteroceptive sensing for terrain-aware
variable-height locomotion.

\section*{Acknowledgment}
% [TODO: Add acknowledgments if applicable.]

\bibliographystyle{IEEEtran}
\begin{thebibliography}{99}

\bibitem{ppo}
J. Schulman, F. Wolski, P. Dhariwal, A. Radford, and O. Klimov,
``Proximal Policy Optimization Algorithms,''
\emph{arXiv preprint arXiv:1707.06347}, 2017.

\bibitem{mujoco}
E. Todorov, T. Erez, and Y. Tassa,
``MuJoCo: A physics engine for model-based control,''
in \emph{Proc. IEEE/RSJ Int. Conf. Intelligent Robots and Systems (IROS)},
2012, pp. 5026--5033.

\bibitem{mujoco_mjx_docs}
``MuJoCo Documentation: MuJoCo XLA (MJX),''
[Online]. Available:
\url{https://mujoco.readthedocs.io/en/latest/mjx.html}.
Accessed: Apr. 2026.

\bibitem{jax}
J. Bradbury \emph{et al.},
``JAX: composable transformations of Python+NumPy programs,''
2018. [Online]. Available: \url{http://github.com/jax-ml/jax}

\bibitem{flax}
J. Heek, A. Levskaya, A. Oliver, \emph{et al.},
``Flax: A neural network library and ecosystem for JAX,''
2024. [Online]. Available: \url{http://github.com/google/flax}

\bibitem{gae}
J. Schulman, P. Moritz, S. Levine, M. Jordan, and P. Abbeel,
``High-Dimensional Continuous Control Using Generalized Advantage
Estimation,''
in \emph{Proc. Int. Conf. Learning Representations (ICLR)}, 2016.

\bibitem{welford1962}
B. P. Welford,
``Note on a Method for Calculating Corrected Sums of Squares
and Products,''
\emph{Technometrics}, vol.~4, no.~3, pp. 419--420, 1962.

\bibitem{tobin2017domain}
J. Tobin, R. Fong, A. Ray, J. Schneider, W. Zaremba, and P. Abbeel,
``Domain Randomization for Transferring Deep Neural Networks from
Simulation to the Real World,''
in \emph{Proc. IEEE/RSJ Int. Conf. Intelligent Robots and Systems (IROS)},
2017, pp. 23--30.

\bibitem{bengio2009curriculum}
Y. Bengio, J. Louradour, R. Collobert, and J. Weston,
``Curriculum Learning,''
in \emph{Proc. Int. Conf. Machine Learning (ICML)}, 2009, pp. 41--48.

\bibitem{klemm2019ascento}
V. Klemm \emph{et al.},
``Ascento: A Two-Wheeled Jumping Robot,''
in \emph{Proc. IEEE Int. Conf. Robotics and Automation (ICRA)}, 2019.

\bibitem{klemm2020lqrwbc}
V. Klemm, A. Morra, L. Gulich, \emph{et al.},
``LQR-Assisted Whole-Body Control of a Wheeled Bipedal Robot
with Kinematic Loops,''
\emph{IEEE Robotics and Automation Letters},
vol.~5, no.~2, pp. 3745--3752, 2020.

\bibitem{hsu2022wbrfuzzy}
C.-F. Hsu, B.-R. Chen, and Z.-L. Lin,
``Implementation and Control of a Wheeled Bipedal Robot Using
a Fuzzy Logic Approach,''
\emph{Actuators}, vol.~11, no.~12, p. 357, 2022.

\bibitem{li2021wheelbiped}
X. Li \emph{et al.},
``Dynamic wheeled motion control of wheel-biped transformable robots,''
\emph{Biomimetic Intelligence and Robotics}, 2021.

\bibitem{rudin2022walk}
N. Rudin, D. Hoeller, P. Reist, and M. Hutter,
``Learning to Walk in Minutes Using Massively Parallel Deep Reinforcement Learning,''
in \emph{Proc. Conf. Robot Learning (CoRL)}, vol.~164, pp. 91--100, 2022.

\bibitem{lee2020learning}
J. Lee, J. Hwangbo, L. Wellhausen, V. Koltun, and M. Hutter,
``Learning Quadrupedal Locomotion over Challenging Terrain,''
\emph{Science Robotics}, vol.~5, no.~47, p.~eabc5986, 2020.

\bibitem{kumar2021rma}
A. Kumar, Z. Fu, D. Pathak, and J. Malik,
``RMA: Rapid Motor Adaptation for Legged Robots,''
in \emph{Proc. Robotics: Science and Systems (RSS)}, 2021.

\bibitem{hwangbo2019learning}
J. Hwangbo \emph{et al.},
``Learning Agile and Dynamic Motor Skills for Legged Robots,''
\emph{Science Robotics}, vol.~4, no.~26, p.~eaau5872, 2019.

\bibitem{miki2022wild}
T. Miki \emph{et al.},
``Learning Robust Perceptive Locomotion for Quadrupedal Robots in the Wild,''
\emph{Science Robotics}, vol.~7, no.~62, p.~eabk2822, 2022.

\bibitem{lee2024robustnavigation}
J. Lee \emph{et al.},
``Learning Robust Autonomous Navigation and Locomotion for Wheeled-Legged Robots,''
\emph{Science Robotics}, vol.~9, 2024.

\bibitem{vollenweider2023advancedskills}
E. Vollenweider \emph{et al.},
``Advanced Skills through Multiple Adversarial Motion Priors in Reinforcement Learning,''
in \emph{Proc. IEEE Int. Conf. Robotics and Automation (ICRA)}, pp. 5120--5126, 2023.

\bibitem{chamorro2024stairclimbing}
I. Chamorro, J. Arrizabalaga, and M. Hutter,
``Reinforcement Learning for Blind Stair Climbing with Legged and Wheeled-Legged Robots,''
in \emph{Proc. IEEE Int. Conf. Robotics and Automation (ICRA)}, 2024.

\bibitem{whleaper2024}
J. Zhu, S. Zhao, and E. Yoshida,
``Whleaper: A 10-DOF Flexible Bipedal Wheeled Robot,''
in \emph{Proc. IEEE/RSJ Int. Conf. Intelligent Robots and Systems (IROS)},
pp. 11272--11277, 2024.

\bibitem{zhu2022hierarchical}
J. Zhu and M. Hayashibe,
``Reinforcement Learning Based Hierarchical Control for Path Tracking of a
Wheeled Bipedal Robot with Sim-to-Real Framework,''
in \emph{Proc. IEEE/SICE Int. Symp. System Integration (SII)}, 2022.

\bibitem{diablo2024}
S. Liu \emph{et al.},
``DIABLO: A 6-DoF Wheeled Bipedal Robot Composed Entirely of Direct-Drive Joints,''
\emph{arXiv preprint arXiv:2407.21500}, 2024.

\bibitem{portelas2020acl}
R. Portelas, C. Colas, L. Weng, K. Hofmann, and P.-Y. Oudeyer,
``Automatic Curriculum Learning for Deep RL: A Short Survey,''
in \emph{Proc. Int. Joint Conf. Artificial Intelligence (IJCAI)}, 2020.

\bibitem{portelas2020teacher}
R. Portelas, C. Colas, K. Hofmann, and P.-Y. Oudeyer,
``Teacher Algorithms for Curriculum Learning of Deep RL in Continuously
Parameterized Environments,''
in \emph{Proc. Conf. Robot Learning (CoRL)}, vol.~100, pp. 835--853, 2020.

\bibitem{wang2019poet}
R. Wang, J. Lehman, J. Clune, and K. O. Stanley,
``Paired Open-Ended Trailblazer (POET): Endlessly Generating Increasingly
Complex and Diverse Learning Environments and Their Solutions,''
\emph{arXiv preprint arXiv:1901.01753}, 2019.

\bibitem{peng2018simtoreal}
X. B. Peng, M. Andrychowicz, W. Zaremba, and P. Abbeel,
``Sim-to-Real Transfer of Robotic Control with Dynamics Randomization,''
in \emph{Proc. IEEE Int. Conf. Robotics and Automation (ICRA)},
pp. 3803--3810, 2018.

\bibitem{tan2018simtoreal}
J. Tan \emph{et al.},
``Sim-to-Real: Learning Agile Locomotion for Quadruped Robots,''
in \emph{Proc. Robotics: Science and Systems (RSS)}, 2018.

\end{thebibliography}

\end{document}
